============
Installation
============

At the command line::

    $ pip install AddressBook

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv AddressBook
    $ pip install AddressBook
